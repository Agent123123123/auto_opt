{
  "$schema": "https://opencode.ai/config.json",
  "agent": {
    "no-skill": {
      "mode": "primary",
      "description": "Primary agent with skill tool disabled (for automated pipelines that manage their own context)",
      "permission": {
        "skill": "deny",
        "external_directory": "allow"
      }
    }
  }
}
